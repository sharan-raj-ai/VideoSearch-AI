import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Semantic Video Search | AI-Powered Video Intelligence",
  description: "Search through video content using natural language. Powered by advanced AI embeddings and multi-modal understanding.",
  keywords: ["video search", "AI", "semantic search", "machine learning", "video intelligence"],
  authors: [{ name: "Sharan Raj VK" }],
  openGraph: {
    title: "Semantic Video Search",
    description: "AI-Powered Video Intelligence Platform",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link
          href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500&display=swap"
          rel="stylesheet"
        />
      </head>
      <body className="antialiased">
        {children}
      </body>
    </html>
  );
}
